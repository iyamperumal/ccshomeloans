@model bool?
<div style="padding-top:4px; margin-left: 1px !important" class="checkbox">

    @{
        bool b;
        object o = @ViewData.TemplateInfo.FormattedModelValue;
        if (Model == null) b = false;
        else b = (bool)@ViewData.TemplateInfo.FormattedModelValue;
       
    }
   


    @Html.CheckBox("", b)

</div>
@*Html.TextBox("", ViewData.TemplateInfo.FormattedModelValue, new { @class = "form-control input-lg date_day" } ViewData.TemplateInfo.FormattedModelValue )*@
